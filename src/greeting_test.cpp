#include <userver/utest/utest.hpp>

// Placeholder test — old greeting tests removed,
// new handler tests are in the testsuite (Python functional tests).
UTEST(Placeholder, Compiles) { EXPECT_EQ(1, 1); }